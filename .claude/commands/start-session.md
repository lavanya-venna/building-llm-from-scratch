1. Read `pyproject.toml` and extract the current `version` field.
2. Read `doc/v{version}_changes.md`. If it doesn't exist, say so.
3. If it exists, summarize: current phase, what's implemented vs pending, open items, blockers, and any unverified work from the last session.
4. Check if previous version docs exist in `doc/` — note anything marked "deferred to next version."
5. Ask me what I want to work on this session.